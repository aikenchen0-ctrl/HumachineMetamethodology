# skills

这个目录提供给 Codex 官方 GitHub 安装器使用的稳定 skill 子路径。

当前可安装入口：

- `skills/HMM`

推荐安装方式：

```bash
scripts/install-skill-from-github.py --repo aikenchen0-ctrl/HumachineMetamethodology --path skills/HMM
```

不要把仓库根目录直接当作官方安装器的 `--path`。
安装完成后重启 Codex。
