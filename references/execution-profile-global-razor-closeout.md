# execution-profile-global-razor-closeout

## Purpose

This file records the current global razor conclusion for the execution-profile work.

Its job is not to introduce new structure.

Its job is to say clearly:

1. what is already sufficient,
2. what should now stop,
3. and what the single highest-value next move is.

## Current Global State

Treat the repository as already sufficient on the preparation side.

The execution-profile stack now has:

1. repository-side validation context
2. non-self comparison plan
3. starter task packets
4. ready-to-run briefs
5. prefilled scorecards
6. execution runbook
7. result report template
8. navigation index

This means the repository is no longer blocked on missing preparation surfaces.

## Razor Conclusion

The current bottleneck is no longer repository structure.

The current bottleneck is now execution.

So the correct razor is:

- stop expanding repository-side preparation by default
- stop adding new support files by default
- stop polishing wording by default
- and switch to non-self comparison execution

## What Should Stop Now

Unless a real contradiction appears, stop:

1. adding new execution-profile support files
2. adding more starter packets
3. refining scorecard wording
4. refining example wording
5. reopening shell or contract structure

## What Is Still Allowed

Only these now justify more repository-side edits:

1. a real run reveals a missing field in a scorecard
2. a real run reveals a missing artifact expectation
3. a real run reveals a hard boundary break
4. a real run proves one of the current packets is unusable

If none of those happens, do not reopen repository work.

## Single Highest-Value Next Move

Execute run `01`.

Use:

- `references/execution-profile-assets-index.md`
- `references/execution-profile-non-self-run-01-problem-solving-brief.md`
- `references/execution-profile-non-self-run-01-problem-solving-scorecard.json`
- `references/execution-profile-non-self-runbook.md`
- `references/execution-profile-non-self-comparison-result-template.md`

Run order:

1. `autonomous_steady`
2. `autonomous_deep`

## Next Branch After That

If run `01` does not produce a hard fail:

- execute run `02`

If run `01` does produce a hard fail:

- pause execution expansion
- repair only the smallest blocking policy or contract surface

## Final Rule

Until a real run says otherwise:

- repository-side preparation is sufficient
- execution is now the default next move
